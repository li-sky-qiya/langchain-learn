# LangChain Learn

LangChain 学习项目，探索 LangChain 框架与大模型（OpenAI、DeepSeek）的集成应用。

### 配置环境变量

项目使用 `.env` 文件管理 API 密钥等敏感信息，请确保在项目根目录下创建 `.env` 文件并配置必要的变量

## 🚀 快速开始

1. 安装依赖
pip install 'langchain[openai,deepseek]' dotenv

2. 配置 `.env` 文件
3. 运行 `env_utils.py` 加载环境变量
4. 开始 LangChain 学习之旅！

